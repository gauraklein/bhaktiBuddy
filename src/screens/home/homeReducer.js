const homeState = {
    username: "User",
    roundGoal: 16,

}

export const homeReducer = (state = homeState, action) => {

    // switch (action.type) {
    //     // TODO: make this work
    //     case CHANGE_USERNAME:
    //         let newState = {
    //             ...state,
    //             username: action.payload
    //         }
    //         return newState;
        // break;

        // default:
          return state
    //   }
}