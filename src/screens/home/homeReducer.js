const homeState = {
    username: "User",
    roundGoal: 16,
    lifetimeRounds: 108,
    catchUpRounds: 23,
    japaStreak: 4
}

export const homeReducer = (state = homeState, action) => {

    switch (action.type) {

        default:
          return state
      }
}

